/**
 * JSON Input Module
 *
 * Provides types and parsers for AI agents to generate manifests using JSON
 * instead of TypeScript code.
 *
 * @module json
 */

export * from './types'
export * from './parser'
