name: Publish to npm

on:
  push:
    tags:
      - 'v*.*.*'  # Trigger on version tags like v2.0.1, v2.1.0

jobs:
  publish:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # Required to create GitHub releases
      id-token: write  # Required for OIDC trusted publishing & provenance
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm run test:run
      
      - name: Build package
        run: npm run build
      
      - name: Publish to npm (using Trusted Publishing)
        # Using npx to ensure we use npm's built-in OIDC support
        run: npx --yes npm@latest publish --provenance --access public
        # Note: npm 9.5.0+ with --provenance automatically uses OIDC when id-token:write is set
        # The trusted publisher configured on npmjs.com validates the OIDC token
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 📦 Archetype Engine ${{ github.ref_name }}
            
            See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for full details.
            
            ### Installation
            ```bash
            npm install archetype-engine@latest
            ```
            
            ### Verification
            This release includes automatic [npm provenance](https://www.npmjs.com/package/archetype-engine?activeTab=provenance) proving it was built from this exact commit.
          draft: false
          prerelease: false
          generate_release_notes: true
